# Pin the public npm registry.
#
# This repo's lockfile was once regenerated on a machine configured with a
# corporate Artifactory mirror, which rewrote every `resolved` URL to an
# internal host. A fresh clone elsewhere then 401s on every tarball fetch.
# Pinning the registry here keeps regenerated lockfiles portable.
registry=https://registry.npmjs.org/
