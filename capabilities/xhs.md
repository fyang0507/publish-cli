---
schemaVersion: publish.channel-info-source/v1
channel: xhs
displayName: Xiaohongshu / RedNote
---
# Xiaohongshu / RedNote

## CLI boundary

Publish-cli has no Xiaohongshu read or write transport. This entry is a self-contained procedure for an agent using its own persistent headful browser; `publish xhs info` itself does not open or inspect Xiaohongshu and never proves readiness. The agent imports, prepares, and verifies content in that same browser context. The caller owns the choice between a plain long article and the generated-card/image-text representation. A human completes login challenges when required. The calling workflow owns editorial choices and publication authorization.

The supported procedure begins from a long-article source. Direct image-text, video, and podcast staging remain unspecified. This procedure grants no publication authorization by itself. An explicit user request to publish or continue through publication governs the agent-owned workflow; do not stop at a plain draft checkpoint solely because the CLI is draft-oriented.

## Authentication

Open `https://creator.xiaohongshu.com/publish/publish` in one persistent agent-owned browser. Positively identify the authenticated creator UI before drafting. If Xiaohongshu shows QR login or a challenge, ask the human to complete it, then continue in that exact browser context; do not switch to an unrelated browser or infer readiness from cookies or local files.

Drafts are browser-local. Preserve the same browser profile through save and verification, and do not clear its data. Authentication or draft verification is unavailable when the creator UI cannot be identified, the challenge remains incomplete, or the original persistent context cannot be continued.

## Platform specification and gotchas

- Evidence boundary: the editor, import, generated-card, topic, and draft-box facts below come from a live spike completed on 2026-08-30 and a follow-up on 2026-09-06. Xiaohongshu owns current controls, counters, and limits; re-check the live UI and resolve differences before the affected action. An observed maximum of 100 long-article drafts is not a universal limit.
- Prepare `.md`, `.docx`, or `.txt` plus a separate title; Markdown is preferred. Import replaces the entire editor body and does not populate the separate title. If no title was supplied, copy the first Markdown H1, otherwise the first non-empty line. Never invent or silently truncate a title.
- The long-article editor showed a title counter of 64 and a body counter of 10,000, with unknown measurement units. Obey the live counters. In the measured Markdown import, H1/H2, lists, and blockquotes survived; H3 became a paragraph, and bold/italic text survived without styling. Hyperlink labels survived the follow-up import, but all 12 reference URLs lost their links. Disclose this import behavior; when the user accepts reference text without hyperlinks, continue without asking again or adding visible URLs. Preserve linked source Markdown separately. Compare the entire imported result with the source and resolve any other unacceptable loss.
- Representation decision: for a plain-draft-only request, keep the plain long-article representation. Structured topics were available only in the final image-text composer reached through `一键排版` → `下一步`; that path converts the article into generated image cards and can change its template, cover, summary, title, and caption semantics. For a complete post-preparation or publication request, explain the conversion and continue through this native flow unless the user restricted the representation. If the user's constraints conflict with conversion, clarify that conflict. Do not treat silence as acceptance of a requested exception, and never substitute plain pasted hashtags for structured topics.
- Plain long-article route: open `写长文` → `新的创作`, import the source, fill the separate title, and compare the complete editor state with the source. The accepted long-article representation is the exact visible title plus the reviewed body text, block order, and supported structure after the disclosed import behavior; enter `一键排版` when continuing to the full post composer. Use `暂存离开` only when it is still uniquely identifiable as the non-publication long-article draft action. The follow-up observed `保存成功`, `草稿箱(1)`, and an exact 63-block same-profile reopen after `暂存离开`; a toast alone is not success. If the draft action is missing or ambiguous, stop without trying adjacent controls. If its activation or delivery is uncertain, do not repeat it because a draft may already exist; inspect the same-profile draft box instead.
- Generated-card/topic route, within the requested representation: select `一键排版`, review the generated card count, card order/content, template, summary, and cover, then select `下一步` to enter the image-text composer. A measured 225-character fixture became three cards; the follow-up essay became one cover and nine body cards. These counts are observations, not rules. Concatenated body-card text matched the accepted article exactly; also inspect pagination and visual readability. The August spike modified and truncated the title; the September follow-up retained it unchanged. Never silently accept, repair, or reproduce a changed title: show the exact visible result to the caller and require acceptance before making it part of the reviewed representation. The final image-text title limit and measurement remain unverified. Its caption is separate from the generated cards and showed a counter of 1,000 with unknown measurement; use caller-supplied text or write a concise summary/hook when post preparation is authorized. Prefer a witty, source-grounded hook when requested; do not paste article excerpts as a substitute or add unsupported factual claims.
- In the final image-text composer, resolve each requested structured topic through live `话题` suggestions and select only an exact platform match. Verify that it became a distinct topic entity rather than literal `#text`; if the match is absent or ambiguous, stop and return the visible candidates instead of guessing. When the caller delegates post completion, choose a small relevant set through these live suggestions; do not require the user to supply every topic name. The maximum topic count is unknown. The spike observed four selected topic entities survive reload, draft-box reopen, and preview.
- Complete applicable composer fields before terminal handoff: title, all generated images, caption, relevant native topics, and original-content declaration. When the user requests `原创声明`, enable it and verify both the checked control and `已声明原创` preview. Inspect content-type declaration choices and select only an accurate applicable value. Do not invent locations, tagged people, groups, routes, attachments, collections, or campaign associations just to fill every optional field. Keep existing user choices.
- Editing caption text must preserve selected native topic entities. In the follow-up, selecting an exact individual paragraph's text and pasting its replacement worked while filling a child paragraph failed; full-editor replacement would remove topic nodes. Read the live editor first, use supported selection/editing APIs, and verify the revised caption and every topic entity afterward.
- Browser interaction evidence: the follow-up allowed text editing while some clicks returned without changing the page; a human click opened the topic menu and later selections and the original declaration were visible. This does not establish a platform-wide click failure or its root cause. Inspect the live DOM, accessibility tree, screenshot, and the browser tool's documented troubleshooting; retarget against observed controls. A tool returning without error is not proof of mutation. Do not invoke hidden handlers or replay private APIs. If a human step is necessary, ask for the exact blocked action and resume in the same context.
- Terminal controls: the follow-up final image-text composer visibly showed `暂存离开` and `发布`, but did not verify an image-text save/reopen or successful automated publication. Establish the current control's role before using it; visibility alone does not prove successful delivery. Missing image-text draft-save calibration does not block independently authorized caption, topic, declaration, or review work. For a draft request, use only a positively identified non-publication save action and verify through the draft box. For authorized publication, verify the completed composer, submit once, and check an authoritative result/post record; a `published=true` URL alone is insufficient. Never retry an uncertain terminal submission without reconciling its result first.
- Draft verification always requires the same persistent browser and a canonical `草稿箱` reopen; a toast, autosave label, URL change, reload survival, or return to a landing page alone is insufficient. For a plain draft, reopen the corresponding long-article entry and require the exact accepted title, full body text, block order, and supported structure. For a generated-card/topic draft whose safe terminal save was separately established, reopen the corresponding draft and require the accepted card count/order/content, template/cover state, exact accepted visible title, caption, and every selected topic entity. A missing, ambiguous, or differing entry is unverified: stop and do not repeat the save.
- Browser-local means the draft is not a portable cloud guarantee: losing or clearing the agent-owned browser data can remove it. Preserve that profile for human review and report this limitation with the draft handoff.
- Publication authorization is separate from representation and draft verification. Do not publish, schedule, or change visibility beyond the user's authorization. When publication is requested and permitted, continue from the completed composer rather than treating a verified draft as the final deliverable. Report the actual verified outcome and distinguish user actions from agent actions.
